#include <stdio.h>
#include "main.h"

/**
 * add - Entry point
 *
 * @integer_1: one of the integers to be added.
 * @integer_2: one of the inegers to be added.
 *
 * Description: adds two integers and return the result.
 *
 * Return: Always result (success).
 */

int add(int integer_1, int integer_2)
{
	int result;

	result = integer_1 + integer_2;

	return (result);
}
