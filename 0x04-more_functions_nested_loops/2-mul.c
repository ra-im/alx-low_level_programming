#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: one of multiples
 * @b: one of the multiples
 * Return: result (success).
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
